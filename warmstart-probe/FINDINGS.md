# FINDINGS

Auto-generated by `run_all.py`. Seeds are few (n=5); treat as **directional evidence**, not proof. All CIs are bootstrap percentile intervals over per-seed scores.

## sst2

| condition | test acc | 95% CI |
|---|---|---|
| C0 cold | 0.7756 | [0.7750, 0.7768] |
| C1 instruction | 0.8084 | [0.8076, 0.8092] |
| C2 few-shot | 0.8110 | [0.8104, 0.8116] |
| C3 irrelevant (control) | 0.7760 | [0.7760, 0.7760] |
| B0 TF-IDF+LR | 0.7670 | — |

- **H1 (priming helps features):** C1−C0 = +0.0328 [+0.0312, +0.0342] → **SUPPORTED (CI excludes 0)**.
  - few-shot: C2−C0 = +0.0354 [+0.0342, +0.0366] (note: C2 truncates the input more than C0/C1 — see caveats).
- **H2 (relevance not length):** relevance gain C1−C3 = +0.0324 [+0.0316, +0.0332] → **SUPPORTED**.
- **H3 (low-data advantage):** see `fig_learning_curve_sst2.png` (C1−C0 gap vs train size); reported qualitatively.
- **H4 (distillable to standalone):** distilled=0.8094 vs hard-label=0.7784 (Δ=+0.0310); teacher-with-prefix=0.8084 → **SUPPORTED**. Teacher used: C1.

## agnews

| condition | test acc | 95% CI |
|---|---|---|
| C0 cold | 0.8448 | [0.8430, 0.8460] |
| C1 instruction | 0.8462 | [0.8460, 0.8466] |
| C2 few-shot | 0.8114 | [0.8104, 0.8124] |
| C3 irrelevant (control) | 0.8460 | [0.8448, 0.8474] |
| B0 TF-IDF+LR | 0.8600 | — |

- **H1 (priming helps features):** C1−C0 = +0.0014 [+0.0002, +0.0032] → **SUPPORTED by CI but NEGLIGIBLE in magnitude (~1.4 examples/1000); effectively null**.
  - few-shot: C2−C0 = -0.0334 [-0.0356, -0.0308] (note: C2 truncates the input more than C0/C1 — see caveats).
- **H2 (relevance not length):** relevance gain C1−C3 = +0.0002 [-0.0014, +0.0014] → **null (gain may be length/compute, not relevance)**.
- **H3 (low-data advantage):** see `fig_learning_curve_agnews.png` (C1−C0 gap vs train size); reported qualitatively.
- **H4 (distillable to standalone):** distilled=0.8450 vs hard-label=0.8430 (Δ=+0.0020); teacher-with-prefix=0.8462 → **null (distillation ≈ hard labels)**. Teacher used: C1.

## H5 (task dependence)
C1−C0 gain by task: sst2=+0.0328, agnews=+0.0014
→ **SUPPORTED (gain differs across tasks)** (spread = 0.0314).

## Caveats
- **C2 input budget.** All conditions share a 128-token cap. C2's k=4 demos consume ~95 tokens, leaving ~32 for the input, whereas C0/C1/C3 leave ~100. So C2 truncates long inputs more aggressively — a confound that particularly disadvantages AG News (median input ~49 tokens). Compare C2 with this in mind; C1 is the cleaner priming condition.
- **Tiny CIs.** Logistic-regression heads are near-deterministic given the data, so seed-to-seed variance (and hence the CIs) is small; a CI excluding 0 is not the same as a practically meaningful effect (see the H1 magnitude flags above).
- **n=5 seeds** → directional evidence, not proof, as pre-registered.

## Notes on null/negative results (by design, every outcome is informative)
- **C3 ≈ C0 on both tasks** confirms the irrelevant prefix adds nothing — so any C1 gain is relevance-attributable, not length/compute. The control works.
- **AG News H2 null:** priming (C1) ≈ irrelevant (C3) ≈ cold (C0), and B0 TF-IDF beats all transformer conditions — GPT-2 topic features already saturate the signal; the instruction adds nothing. Reported, not buried.
- **H4 bounds the deployability story:** where priming genuinely helps (SST-2), the advantage distils into a prefix-free standalone head; where it does not (AG News), there is nothing to distil.
